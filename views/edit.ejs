<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class = 'w-full min-h-screen bg-zinc-900 text-white'>
        <a href="/logout" class="absolute top-5 right-5 px-3 py-2 rounded-md bg-red-500">Logout</a>
        <h5 class="mb-5"> Edit Your Post.</h5>
        <form method="post" action="/update/<%= post._id%>">
            <textarea class="resize-none w-full h-32 bg-transparent text-white border-2 border-zinc-800 rounded-md mt-5" placeholder="Write your post here..." name="content"><%=post.content%></textarea>
            <input type="submit" class="px-5 py-2 rounded-md bg-yellow-500 text-black mt-3" value="Update Post">
        </form>
        
    </div>
</body>
</html>